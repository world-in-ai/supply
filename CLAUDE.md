# 작업 규칙 (supply 레포)

이 레포에서 작업할 때 항상 따르는 규칙. (세션마다 재클론되므로 여기에 기록해 지속시킨다.)

## 산출물 전달 방식
- **결과물이 레포에 있을 때, 사용자가 그 내용을 요청하면 해당 md의 하이퍼링크만 준다.**
  채팅에 내용을 다시 붙여넣어 중복 설명하지 않는다.
  - GitHub 링크 형식: `https://github.com/world-in-ai/supply/blob/<branch>/<path>`
  - 예외: 사용자가 "내용을 채팅에 보여줘"라고 명시적으로 요청할 때만 본문을 붙인다.

## 산출물 형식
- 기본은 **리서치 브리프**(요점·표·수치·출처 중심, 산문 최소화). 최종본은 사용자가 워드로 직접 작성한다.
- 완성형 산문 보고서는 사용자가 명시적으로 요청할 때만 작성한다.
- 템플릿: `templates/research-brief-template.md`(기본), `templates/report-template.md`(완성형).

## 저장 위치·폴더 규칙
- 결과물은 레포에 저장한다. `reports/YYYY-MM_슬러그/` 규칙, `research/`는 슬러그 1:1 대응.
- 자세한 폴더 운영 원칙은 `README.md` 참조.

## PR·모니터링
- PR 자동 감시(주기적 셀프 체크인 등)는 하지 않는다. PR 이벤트가 실제로 도착할 때만 대응한다.

## 참고
- 권한 자동 허용 설정은 `.claude/settings.json`에 있다.
